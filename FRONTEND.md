# Erebor Frontend — Reference

> **What this document is.** A complete, current snapshot of the
> Erebor dashboard implementation at [app/](app/). It documents
> *what is built*, not what we plan to build. For the forward-looking
> plan + open questions see [FRONTEND_PLAN.md](FRONTEND_PLAN.md). For
> spec-vs-code gaps see [MISMATCHES.md](MISMATCHES.md). For program
> background see [OVERVIEW.md](OVERVIEW.md) +
> [SOLANA_VAULT_SPEC.md](SOLANA_VAULT_SPEC.md). For contributor
> guidance see [CLAUDE.md](CLAUDE.md).

---

## 1. What the frontend is

A multi-vault, role-aware dashboard for the `my_project` Anchor
program at id `DXcUni7VCBiLA8MEa2cB4nektLT33Dth62skuiyuwm5B`. Three
audiences, one app:

| Audience      | What they do                                                                                                |
| ------------- | ----------------------------------------------------------------------------------------------------------- |
| **User**      | Browse vaults, deposit / withdraw, inspect aggregate position + allocation pie                              |
| **Admin**     | Create strategies, set per-strategy weight, rotate the delegate (AI agent keypair), permanently deactivate  |
| **Authority** | Manual `allocate_to_strategy` / `deallocate_from_strategy` / `rebalance_strategy` / `report_yield`          |

Every vault is qualified by `(token_mint, vault_id)` — the same id on
two mints is a different vault. The active vault is held in the
`VaultProvider` context; routes do not yet encode it in the URL
(`/vault/[chainId]/[address]` from the spec is a roadmap item).

---

## 2. Stack (actually used)

| Layer            | Choice                                                                                                |
| ---------------- | ----------------------------------------------------------------------------------------------------- |
| Framework        | **Next.js 16.2.1** App Router, **TypeScript 6**                                                       |
| Wallet           | **`@solana/wallet-adapter-react` 0.15** + `@solana/wallet-adapter-react-ui` 0.9 + `@solana/wallet-adapter-wallets` 0.19 |
| Chain client     | **`@solana/web3.js` 1.98.4** + **`@coral-xyz/anchor` 0.32.1** + **`@solana/spl-token` 0.4.14**          |
| UI               | **Tailwind 4** + custom CSS variables (no shadcn). Radix is **not** used (the EVM playbook used it).   |
| Charts           | **Recharts 3** (donut for allocation)                                                                  |
| Toasts           | **react-hot-toast** wired in [app/src/app/layout.tsx](app/src/app/layout.tsx) and surfaced through [app/src/components/shared/TxToast.tsx](app/src/components/shared/TxToast.tsx) |
| ABI              | **Anchor IDL** — `target/idl/my_project.json` (regenerated by `anchor build`) is hand-mirrored to [app/src/idl/my_project.ts](app/src/idl/my_project.ts) |
| RPC              | Direct connection via `@solana/web3.js` to whatever `NEXT_PUBLIC_RPC_URL` points at — **no server-side proxy**     |

Notable pins / quirks:

- `@coral-xyz/anchor@0.32.1` matches the program toolchain pinned in
  [Anchor.toml](Anchor.toml) and [Cargo.toml](Cargo.toml). Bumping
  the program also requires a frontend bump.
- `next@^16.2.1` + `react@^19.2.4` + `typescript@^6.0.2` — relatively
  new versions; mind the React 19 strict-mode behavior in client
  components.
- The Next.js root layout defers state by using a **client provider
  tree** (`SolanaProvider` → `VaultProvider`), avoiding SSR
  hydration mismatches with the wallet adapter (see
  [app/src/app/layout.tsx](app/src/app/layout.tsx)).

---

## 3. Directory layout (actual)

```
app/
  next.config.ts · tsconfig.json · postcss.config.mjs · package.json
  src/
    app/
      layout.tsx                                 # SolanaProvider + VaultProvider + Navbar + Toaster
      page.tsx                                   # multi-vault dashboard (vault list + stats + deposit/withdraw + pie + position)
      globals.css                                # Tailwind base + dark CSS vars
      admin/
        page.tsx                                 # admin/authority panel (create + rebalance-all + per-strategy cards)

    components/
      admin/
        AdminGuard.tsx                           # role gate wrapper (all-or-nothing) for /admin
        AllocationChart.tsx                      # Recharts donut over reserve + active strategies
        CreateStrategyForm.tsx                   # createStrategy(delegate) + checklist UI
        StrategyCard.tsx                         # per-strategy card: weight + delegate rotate + deactivate + manual allocate/deallocate
        StrategyList.tsx                         # admin index of strategies
      layout/
        Navbar.tsx                               # brand + VaultSelector + WalletMultiButton + admin link (when admin/authority)
        VaultSelector.tsx                        # popover dropdown of all entries in VAULT_REGISTRY
      providers/
        SolanaProvider.tsx                       # ConnectionProvider + WalletProvider + WalletModalProvider
        VaultProvider.tsx                        # active vault + PDA derivations + on-chain VaultState fetch
      shared/
        AmountInput.tsx                          # token amount input (+ Max button)
        TxToast.tsx                              # success / error toast helpers
      vault/
        DepositForm.tsx                          # deposit(amount) flow
        WithdrawForm.tsx                         # withdraw(shares_to_burn) flow
        UserPosition.tsx                         # user share balance + estimated underlying value
        VaultList.tsx                            # registry list (clickable rows)
        VaultStats.tsx                           # asset symbol + TVL + share price

    hooks/
      useVaultProgram.ts                         # build the Anchor Program (read-only without wallet, write with)
      useDeposit.ts                              # deposit(amount)
      useWithdraw.ts                             # withdraw(shares_to_burn)
      useStrategies.ts                           # fetch every StrategyAllocation account for the active vault
      useUserPosition.ts                         # user share + token balances; refresh interval
      useAdminActions.ts                         # createStrategy / deactivateStrategy / updateDelegate / setStrategyWeight
      useAuthorityActions.ts                     # allocate / deallocate / reportYield / rebalanceStrategy / rebalanceAll

    lib/
      constants.ts                               # PROGRAM_ID, CLUSTERS, getCluster/getRpcUrl/getTokenMint, VAULT_REGISTRY, getExplorerUrl
      pda.ts                                     # deriveVaultPda / deriveShareMintPda / deriveReserveAta / deriveStrategyPda / deriveStrategyTokenPda / deriveUserAta
      format.ts                                  # formatTokenAmount / formatSharePrice / formatPercent / truncateAddress / parseTokenInput
      idl.ts                                     # one-line re-export of the IDL

    idl/
      my_project.ts                              # Anchor IDL TS bindings (mirror of target/idl/my_project.json)
```

> Solidity changes are not relevant — this is a Solana program. After
> any change to [programs/my_project/src/lib.rs](programs/my_project/src/lib.rs)
> regenerate `target/idl/my_project.json` (`anchor build`) and update
> [app/src/idl/my_project.ts](app/src/idl/my_project.ts).

---

## 4. Routes

### `/` — multi-vault dashboard

[`DashboardPage`](app/src/app/page.tsx) renders:

1. **`VaultList`** — clickable rows for every entry in
   `VAULT_REGISTRY`. Selecting a row updates the
   `VaultProvider` active vault.
2. **Active vault header** — name + token symbol pill.
3. **`VaultStats`** — asset symbol, TVL (= `total_deposited`), share
   price (computed via [format.ts:formatSharePrice](app/src/lib/format.ts)),
   strategy count.
4. **Two-column grid** (`lg:grid-cols-2`): left = a deposit/withdraw
   tab toggle wrapping `DepositForm` / `WithdrawForm`; right =
   `UserPosition`.
5. **`AllocationChart`** — Recharts donut, full-width below the grid.

The dashboard does *not* have an activity feed — blocked on the
program emitting `#[event]` records. See
[MISMATCHES.md §2.5](MISMATCHES.md).

### `/admin` — admin/authority surface

[`/admin/page.tsx`](app/src/app/admin/page.tsx) wraps everything in
[`AdminGuard`](app/src/components/admin/AdminGuard.tsx), which:

- Returns the children unchanged if `publicKey` matches `vault.admin`
  or `vault.authority`.
- Renders a "Not authorized" banner otherwise.

Inside the guard:

- [`CreateStrategyForm`](app/src/components/admin/CreateStrategyForm.tsx) — paste a delegate pubkey + an anti-theft checklist (UI only) → call `createStrategy(delegate)`.
- A "Rebalance all" button → loops over active strategies and calls `rebalanceStrategy` for each.
- [`StrategyList`](app/src/components/admin/StrategyList.tsx) — grid of [`StrategyCard`](app/src/components/admin/StrategyCard.tsx) entries, each exposing per-strategy weight, delegate rotation, deactivation, and manual allocate / deallocate.

There is **no** spec-style `/vault/[chainId]/[address]` per-vault
route, no per-strategy `/vault/.../strategy/[id]` route, no
`/vault/.../admin/strategy/[id]` route. Everything is
context-driven by `VaultProvider`. Adding URL-encoded vault selection
is a roadmap item ([FRONTEND_PLAN.md](FRONTEND_PLAN.md)).

---

## 5. Vault registry

The registry is **build-time** today; it will graduate to an on-chain
registry without UI rewrites once a `VaultFactory`-equivalent ships
(deferred — see [SOLANA_VAULT_SPEC.md §15](SOLANA_VAULT_SPEC.md)).

[`app/src/lib/constants.ts`](app/src/lib/constants.ts):

```ts
export interface VaultEntry {
  name: string;
  tokenMint: PublicKey;
  tokenSymbol: string;
  tokenDecimals: number;
  vaultId: number;
}

export const VAULT_REGISTRY: VaultEntry[] = [
  { name: "AT trader agent",  tokenMint: USDC, tokenSymbol: "USDC", tokenDecimals: 6, vaultId: 0 },
  { name: "Conservative",     tokenMint: USDC, tokenSymbol: "USDC", tokenDecimals: 6, vaultId: 1 },
  { name: "Aggressive Vault", tokenMint: USDC, tokenSymbol: "USDC", tokenDecimals: 6, vaultId: 2 },
  { name: "Stablecoin Yield", tokenMint: USDC, tokenSymbol: "USDC", tokenDecimals: 6, vaultId: 3 },
  { name: "DeFi Alpha",       tokenMint: USDC, tokenSymbol: "USDC", tokenDecimals: 6, vaultId: 4 },
];
```

(`USDC` is the devnet test mint
`45AbULTJqK9dpDNDQMb3fe9ojPwc53gr7uUsqHNwkDUY`.)

PDAs derive in [`app/src/lib/pda.ts`](app/src/lib/pda.ts):

| Helper                                    | Seeds                                                                |
| ----------------------------------------- | -------------------------------------------------------------------- |
| `deriveVaultPda(token_mint, vault_id)`    | `["vault", token_mint, &vault_id.to_le_bytes()]`                     |
| `deriveShareMintPda(vault_state)`         | `["shares", vault_state]`                                            |
| `deriveStrategyPda(vault_state, id)`      | `["strategy", vault_state, &id.to_le_bytes()]`                       |
| `deriveStrategyTokenPda(vault_state, id)` | `["strategy_token", vault_state, &id.to_le_bytes()]`                 |
| `deriveReserveAta(vault_state, mint)`     | `getAssociatedTokenAddressSync(mint, vault_state, /* allowOwnerOffCurve */ true)` |
| `deriveUserAta(mint, owner)`              | `getAssociatedTokenAddressSync(mint, owner)`                         |

There is **no** `AddCustomVaultDialog` today — adding a vault means
editing the registry. See [FRONTEND_PLAN.md](FRONTEND_PLAN.md) for the
runtime-add roadmap item.

---

## 6. RPC + cluster selection

No proxy. `@solana/web3.js`'s `Connection` is constructed by
[`SolanaProvider`](app/src/components/providers/SolanaProvider.tsx)
from `getRpcUrl()` (defaults to the cluster's public RPC). Cluster is
resolved from `NEXT_PUBLIC_CLUSTER` (`devnet` | `mainnet-beta`,
defaults to `devnet`). Override with `NEXT_PUBLIC_RPC_URL` to point
at a private RPC (Helius / Triton / a personal validator).

Caveats:

- Public devnet RPC throttles `getProgramAccounts`; the strategy fan-out
  query in [`useStrategies`](app/src/hooks/useStrategies.ts) will
  be slow on cold start. A private RPC is recommended for demo
  recordings.
- `NEXT_PUBLIC_TOKEN_MINT` overrides the default asset mint without
  editing the registry — useful for testing on a fresh devnet token.

---

## 7. Public feature inventory

### Wallet + navigation

- [`Navbar`](app/src/components/layout/Navbar.tsx) — sticky top
  bar with the Erebor brand, the active-vault selector, the
  `WalletMultiButton`, and (for admins/authorities) a link to
  `/admin`.
- [`VaultSelector`](app/src/components/layout/VaultSelector.tsx) —
  popover dropdown of every entry in `VAULT_REGISTRY`. Highlights
  the active vault from `VaultProvider`.

### Deposit

[`DepositForm`](app/src/components/vault/DepositForm.tsx). Flow
(via [`useDeposit`](app/src/hooks/useDeposit.ts)):

1. User enters an amount; `parseTokenInput` validates against
   `tokenDecimals`.
2. The **Max** button on the embedded
   [`AmountInput`](app/src/components/shared/AmountInput.tsx) fills
   the wallet token balance.
3. `program.methods.deposit(amount).accountsStrict({…}).rpc()` builds
   and sends. The vault PDA signs the `mint_to`; the user signs the
   token transfer in.
4. On success, surface a toast and refetch
   [`useUserPosition`](app/src/hooks/useUserPosition.ts) +
   [`useStrategies`](app/src/hooks/useStrategies.ts).

There is **no separate `approve` step** — Solana doesn't use the
ERC-20 allowance pattern; the token transfer is signed inline by the
user.

### Withdraw

[`WithdrawForm`](app/src/components/vault/WithdrawForm.tsx). Mirror
flow via [`useWithdraw`](app/src/hooks/useWithdraw.ts) — input is
*shares to burn*, the program computes the corresponding underlying.

⚠ If the reserve can't cover the withdrawal, the call reverts with
`InsufficientReserve` (the program does not auto-pull from
strategies). The form surfaces the error message. See
[MISMATCHES.md §2.8](MISMATCHES.md).

### Allocation pie

[`AllocationChart`](app/src/components/admin/AllocationChart.tsx) —
Recharts donut with one slice for the reserve plus one per active
strategy. Center overlay shows TVL; legend underneath shows per-slice
percentages.

### User position card

[`UserPosition`](app/src/components/vault/UserPosition.tsx) —
shares balance + estimated underlying value (computed locally from
`shares * total_deposited / share_supply`). Refreshes on a polling
interval.

### Vault stats

[`VaultStats`](app/src/components/vault/VaultStats.tsx) — asset
symbol, TVL, share price (`formatSharePrice`), strategy count.

### Activity feed

✅ Shipped at [app/src/components/vault/ActivityFeed.tsx](app/src/components/vault/ActivityFeed.tsx).
Decodes Anchor `#[event]` records (added in Phase 2) via the program's
`BorshEventCoder`. Bootstraps from `getSignaturesForAddress` +
`getTransaction` for the most recent ~25 program transactions, then
subscribes to `connection.onLogs` for live updates. Filters to events
that reference the active vault PDA. Caps the visible list at 20 rows.
Allowed-action / value-source events will appear automatically once
those features ship.

### Pause UI

✅ Shipped. [PausedBanner.tsx](app/src/components/vault/PausedBanner.tsx)
shows a destructive banner across `/` and `/admin` whenever
`vault_state.paused` is true (plus an explanation that withdrawals
stay open). The admin-only [PauseToggle.tsx](app/src/components/admin/PauseToggle.tsx)
exposes a single button to flip the flag (disable-not-hide via
[useRoles.ts](app/src/hooks/useRoles.ts) — non-admins see the control
but it's `disabled`).

### Sum-cap weight warning

✅ Shipped in [StrategyList.tsx](app/src/components/admin/StrategyList.tsx).
Running progress bar above the cards shows the sum of `target_weight_bps`
across active strategies. Warns when over-allocated (the program does
not enforce the sum cap — see [MISMATCHES.md §2.2](MISMATCHES.md));
shows the reserve buffer % when under-allocated.

---

## 8. Admin / authority feature inventory

### Create strategy

[`CreateStrategyForm`](app/src/components/admin/CreateStrategyForm.tsx) →
`createStrategy(delegate)` via
[`useAdminActions`](app/src/hooks/useAdminActions.ts). Validates
the delegate pubkey on input; the form also surfaces a manual
"checklist" (review code + audit, track record, key security) — UI
only, not enforced on-chain.

### Per-strategy card

[`StrategyCard`](app/src/components/admin/StrategyCard.tsx)
exposes:

- **Weight slider** — `setStrategyWeight(weight_bps)`. Steps of 100,
  capped at 10 000 per strategy. The contract does not cap the *sum*
  across strategies; the UI does not warn either (open question — see
  [FRONTEND_PLAN.md](FRONTEND_PLAN.md)).
- **Update delegate** — `updateStrategyDelegate(new_delegate)`.
- **Deactivate** — `deactivateStrategy()`. Permanent.
- **Allocate / deallocate** — manual amounts via
  [`useAuthorityActions`](app/src/hooks/useAuthorityActions.ts).

### Strategy list

[`StrategyList`](app/src/components/admin/StrategyList.tsx) — grid
of cards plus the rebalance-all action.

### Authority manual rebalance

A **`rebalanceAll`** button in
[`/admin/page.tsx`](app/src/app/admin/page.tsx) loops over active
strategies and calls `rebalanceStrategy(id)` for each. Spec
([§7.6](SOLANA_VAULT_SPEC.md)) wants an authority-only signed-delta
push/pull radio; the UI is currently weight-driven and permissionless
at the program level. See [MISMATCHES.md §2.2](MISMATCHES.md).

### Yield reporting

`reportYield(strategy_id)` is wired in
[`useAuthorityActions`](app/src/hooks/useAuthorityActions.ts) but
not surfaced as a dedicated UI button today; it's invoked by
[`scripts/crank-yield.ts`](scripts/crank-yield.ts) /
[`scripts/simulate-yield.ts`](scripts/simulate-yield.ts) for the
demo.

### Allowed-action whitelist editor / config editor / value-source editor

❌ **Not built.** All three are blocked on the program adding the
underlying `AllowedAction` / `AutoActionConfig` / `ValueSource`
accounts. See [MISMATCHES.md §2.1](MISMATCHES.md).

---

## 9. Role gating

[`AdminGuard`](app/src/components/admin/AdminGuard.tsx) reads the
on-chain `vault_state.admin` and `vault_state.authority` pubkeys
from `VaultProvider` and compares to the connected wallet's
`publicKey`. The guard is **all-or-nothing**: it either passes the
children through or replaces them with a banner. There is no
per-control disable (the EVM playbook calls for "disable, never
hide"; the Solana port hasn't adopted that pattern yet — see
[FRONTEND_PLAN.md](FRONTEND_PLAN.md) and
[MISMATCHES.md §3](MISMATCHES.md)).

There is no `useRoles`-style hook today; role state is pulled from
`VaultProvider` directly.

---

## 10. Hooks — public surface

| Hook                                                                                        | Purpose                                                                                                                                                |
| ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [`useVaultProgram`](app/src/hooks/useVaultProgram.ts)                                    | Build an Anchor `Program<MyProject>` over the active connection + wallet                                                                                |
| [`useDeposit`](app/src/hooks/useDeposit.ts)                                              | `deposit(amount: BN)` → tx signature                                                                                                                   |
| [`useWithdraw`](app/src/hooks/useWithdraw.ts)                                            | `withdraw(shares_to_burn: BN)` → tx signature                                                                                                          |
| [`useStrategies`](app/src/hooks/useStrategies.ts)                                        | Fetch every `StrategyAllocation` account for the active vault (memcmp filter on `vault`)                                                                |
| [`useUserPosition`](app/src/hooks/useUserPosition.ts)                                    | User share + token balances + estimated underlying; periodic refresh                                                                                   |
| [`useAdminActions`](app/src/hooks/useAdminActions.ts)                                    | `createStrategy(delegate)`, `deactivateStrategy(id)`, `updateDelegate(id, new)`, `setStrategyWeight(id, bps)`                                          |
| [`useAuthorityActions`](app/src/hooks/useAuthorityActions.ts)                            | `allocate(id, amount)`, `deallocate(id, amount)`, `reportYield(id)`, `rebalanceStrategy(id)`, `rebalanceAll(strategies)`                               |

> No `useAllowance` (Solana doesn't have ERC-20 allowance).
> No `useRoles` — `VaultProvider` exposes `admin` / `authority`
> pubkeys directly.
> No `useStrategyAllowedActionsLogs` — no `AllowedAction` PDAs to
> probe, no `ActionExecuted` events to replay (yet).

---

## 11. Selectors and presets

This frontend has no equivalent of the EVM playbook's
`ACTION_PRESETS` / `KNOWN_ALLOWED_ACTION_TARGETS` /
`signatureForSelector` / `CONFIG_PRESETS` / `VALUE_SOURCE_PRESETS`
files. They will appear once the corresponding program features land
(see [FRONTEND_PLAN.md](FRONTEND_PLAN.md) and
[MISMATCHES.md §2.1](MISMATCHES.md)).

---

## 12. Environment variables

| Variable                          | Purpose                                                                                       |
| --------------------------------- | --------------------------------------------------------------------------------------------- |
| `NEXT_PUBLIC_CLUSTER`             | `devnet` (default) or `mainnet-beta`                                                          |
| `NEXT_PUBLIC_RPC_URL`             | Override the cluster default (Helius / Triton / Alchemy SOL)                                  |
| `NEXT_PUBLIC_TOKEN_MINT`          | Override the default asset mint (otherwise: `VAULT_REGISTRY[0].tokenMint`)                    |

There are no server-only env vars today (no proxy). When a proxy
lands ([FRONTEND_PLAN.md](FRONTEND_PLAN.md)), expect a
`NEXT_PUBLIC_*`-mirrored set of server-only variants.

---

## 13. Verification checklist

End-to-end against devnet:

1. `bun install && anchor build` — regenerates `target/idl/`. Update
   [app/src/idl/my_project.ts](app/src/idl/my_project.ts) if the
   IDL changed.
2. `bash scripts/deploy.sh` (only if redeploying the program).
3. `bun scripts/setup-devnet.ts` — full flow: mint test token, init
   vault, create N strategies, simulate yield. Or run the focused
   scripts ([create-vault.ts](scripts/create-vault.ts),
   [create-strategies.ts](scripts/create-strategies.ts),
   [simulate-yield.ts](scripts/simulate-yield.ts)).
4. `cd app && bun install && bun run dev`.
5. Multi-vault paths exercised:
   - [`VaultList`](app/src/components/vault/VaultList.tsx) renders
     every entry in `VAULT_REGISTRY`.
   - [`VaultSelector`](app/src/components/layout/VaultSelector.tsx)
     navigates between them without full reload.
6. Per-vault journeys:
   - **Admin:** create strategy, set weight, deactivate (verify
     permanence).
   - **User:** deposit, observe TVL increase + share price ≈ 1
     initially.
   - [`scripts/simulate-yield.ts`](scripts/simulate-yield.ts) →
     mint to strategy ATA → `report_yield` → share price ticks up →
     redeem and verify realized yield.
   - **Authority:** `allocate` / `deallocate` / `rebalanceAll` and
     confirm the pie updates.

---

## 14. Out of scope / deferred

- **Activity feed** — blocked on `#[event]` emissions
  ([MISMATCHES.md §2.5](MISMATCHES.md)).
- **Allowed-action / config / value-source editors** — blocked on
  the corresponding program features
  ([MISMATCHES.md §2.1](MISMATCHES.md)).
- **Disable-not-hide role gating** — UX upgrade over the current
  all-or-nothing `AdminGuard`
  ([FRONTEND_PLAN.md](FRONTEND_PLAN.md)).
- **Add custom vault dialog** — needs a paste-and-validate path
  against `program.account.vaultState.fetch(vaultPda)` plus
  `localStorage` persistence ([FRONTEND_PLAN.md](FRONTEND_PLAN.md)).
- **Server-side RPC proxy** — only matters if private RPC keys
  shouldn't reach the bundle.
- **Connected-wallet Playwright e2e** — wallet adapter's modal isn't
  headless-friendly; the unblock is a viem-style mocked connector
  using `Keypair.generate()` seeded with anvil-style funded accounts
  on a private validator.
- **Indexer-backed analytics** — running APY, drawdown charts,
  cumulative yield. Needs an indexer once a real vault has production
  history.
- **Sum-cap UX for active strategy weights** — the program does not
  cap the sum at 10 000 bps; the UI does not warn (yet).
- **`/vault/[…]/...` URL-encoded vault selection** — today only
  `/` and `/admin` exist; `VaultProvider` holds the active vault.

---

## 15. Porting notes

This frontend is a Solana port of an EVM dashboard playbook; the
following patterns are network-agnostic and worth carrying forward:

- **Vault is always `(cluster, token_mint, vault_id)` (Solana) or
  `(chainId, address)` (EVM).** Never trust one component alone.
- **Disable-not-hide for role-gated UI.** Roadmap item here, but the
  reasoning carries over.
- **Build-time seed + runtime add via storage.** The
  `localStorage`-backed runtime-add pattern works the same on Solana.
- **Probe-by-known-pairs for non-enumerable on-chain state.** Once
  `AllowedAction` PDAs ship, probe each `(strategy, target_program,
  discriminator)` triple via
  `program.account.allowedAction.fetchNullable`.
- **Push/pull radio for signed deltas.** Don't make the user think
  in signed integers (roadmap).
- **Anti-theft enforcement is documented in the public popover.** The
  `(target_program, discriminator)` whitelist should be visible to
  any user, not just admins, once it exists.
